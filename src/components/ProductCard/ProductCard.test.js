import { render, screen, act } from "@testing-library/react";

describe("Test formulario de productos", ()=>{

    test("Product Card", ()=>{})

    //se renderiza correctamente (con props)
    // spinner en imagen - baja
  



})